# When Weaviate runs in Docker and Ollama on host, use host.docker.internal (no /v1).
# MODEL_API_ENDPOINT=http://host.docker.internal:11434
MODEL_API_ENDPOINT=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text